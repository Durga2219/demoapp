# Server Configuration
server.port=8080

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/test_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.open-in-view=false

# Logging - Optimized for faster startup
logging.level.org.hibernate.SQL=WARN
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=WARN
logging.level.org.springframework.security=WARN
logging.level.org.springframework.web=WARN
logging.level.com.example.demo=INFO

# JWT Configuration
jwt.secret=MyVerySecureAndLongSecretKeyForJWTTokenGeneration12345678901234567890
jwt.expiration=86400000

# Google Maps API Configuration
# Get your API key from: https://console.cloud.google.com/
# Enable Distance Matrix API in your Google Cloud project
google.maps.api.key=YOUR_GOOGLE_MAPS_API_KEY_HERE
google.maps.api.url=https://maps.googleapis.com/maps/api/distancematrix/json

# Note: Replace YOUR_GOOGLE_MAPS_API_KEY_HERE with your actual API key
# Example: AIzaSyB1234567890abcdefghijklmnopqrstuv

# For testing without Google Maps API, you can use fallback calculation
# The system will automatically use fallback distances when API key is not configured

# Razorpay Payment Gateway Configuration - Your Real Keys
razorpay.key.id=rzp_test_RWWSuTDWGevZDY
razorpay.key.secret=2h6gndtGbZdShHA9tGwR2Yvp
razorpay.webhook.secret=whsec_test_webhook_secret

# Test mode - using real Razorpay test API
payment.test.mode=false

# When you get real Razorpay keys, replace above with:
# razorpay.key.id=your_real_key_id
# razorpay.key.secret=your_real_key_secret
# payment.test.mode=false

# Email Configuration - Using d3421163@gmail.com to send OTPs
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=d3421163@gmail.com
spring.mail.password=ftat lrvn ukhn fuht
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# Email Test Mode - DISABLED for real email sending
email.test.mode=false

# OTP Configuration
otp.expiry.minutes=5
otp.length=6

# Performance Optimizations
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.jdbc.batch_versioned_data=true

# Connection Pool Optimization
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000

# Disable unnecessary features for faster startup
spring.jpa.defer-datasource-initialization=false
spring.devtools.restart.enabled=false
